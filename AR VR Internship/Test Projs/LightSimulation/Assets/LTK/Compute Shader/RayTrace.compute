#pragma kernel Trace

struct Ray
{
    float3 position;
    float3 direction;
};

struct Surface
{
    float3 position;
    float4 rotation;

    int reflective;
    int refractive;
    int block;
    int none;


};

StructuredBuffer<Surface> surfaces;
RWTexture2D<float4> result;

[numthreads(8,8,1)]
void Trace (uint3 id : SV_DispatchThreadID)
{

}
